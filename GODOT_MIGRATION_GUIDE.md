# Руководство по миграции на Godot

## Описание проекта

**Fucking Pickup** - 2D игра с элементами выживания и мини-игрой.

### Основные механики:

1. **Игрок (Карапуз)**:
   - Перемещение стрелками (WASD/стрелки)
   - Система HP (3 начальных)
   - Система энергии (100 начальных, тратится на движение)
   - Система прокачки (уровни, улучшения)

2. **Локации**:
   - **Пещера** - начальная локация
     - Отец (NPC) с диалогами
     - Выход в кухню (справа)
     - Выход в мини-игру (слева) - перехватывается отцом
   - **Кухня** - вторая локация
     - Мама (NPC) с диалогами
     - Возврат в пещеру
   - **Мини-игра "Обезьянья атака"**:
     - Выбор стартовой позиции (Север/Юг/Запад/Восток)
     - Ограничения движения в зависимости от позиции:
       - Север/Юг: только влево/вправо
       - Запад/Восток: только вверх/вниз
     - Поляна перед персонажем, окружённая кустами
     - Обезьяны выбегают из кустов на поляну
     - Обезьяны кидают какашки в игрока
     - Задача: уклоняться как можно дольше

3. **Обезьяны**:
   - 4 возраста (1-4)
   - Характеристики зависят от возраста:
     - Скорость броска (throw_speed)
     - Точность (accuracy)
     - Кулдаун между бросками (cooldown)
   - Бегают по полю, выбегают на поляну

4. **Системы**:
   - Health System (HP)
   - Energy System (энергия)
   - Progression System (прокачка, XP, уровни, улучшения)
   - Save System (автосохранение)
   - BrainLink интеграция (опционально)

## Файлы для миграции

### 1. Конфигурационные файлы

```
config/
├── game_config.json          # Настройки окна, игрока, BrainLink
├── balance.json              # Баланс мини-игры и прокачки
└── brainlink_path.json       # Путь к BrainLink (опционально)
```

**game_config.json** содержит:
- Настройки окна (разрешение, fullscreen)
- Параметры игрока (HP, энергия, скорость)
- Настройки BrainLink

**balance.json** содержит:
- Параметры обезьян (количество, распределение по возрастам, характеристики)
- Параметры какашек
- Система прокачки (XP, улучшения)

### 2. Ассеты (спрайты)

```
assets/
├── sprites/
│   ├── player/
│   │   ├── idle/
│   │   │   └── down.png
│   │   └── walk/
│   │       ├── up.png
│   │       ├── down.png
│   │       ├── left.png
│   │       └── right.png
│   ├── npc/
│   │   ├── father.png
│   │   ├── mother.png
│   │   └── portraits/
│   │       ├── father.png
│   │       └── mother.png
│   ├── monkey/
│   │   ├── age1.png
│   │   ├── age2.png
│   │   ├── age3.png
│   │   ├── age4.png
│   │   ├── run/
│   │   │   ├── age1.png
│   │   │   ├── age2.png
│   │   │   ├── age3.png
│   │   │   └── age4.png
│   │   └── throw/
│   │       ├── age1.png
│   │       ├── age2.png
│   │       ├── age3.png
│   │       └── age4.png
│   ├── cave/
│   │   ├── cave_floor.png
│   │   ├── cave_wall.png
│   │   ├── cave_door.png
│   │   └── cave_exit.png
│   ├── kitchen/
│   │   ├── counter.png
│   │   ├── fridge.png
│   │   ├── microwave.png
│   │   ├── sink.png
│   │   └── stove.png
│   ├── minigame/
│   │   └── poop.png
│   └── tiles/
│       ├── floor.png
│       └── wall.png
```

### 3. Исходный код (для понимания логики)

#### Основные классы:

**src/core/game.py** - Главный класс игры:
- Инициализация
- Игровой цикл
- Управление сценами
- Обработка входов/выходов
- Диалоги

**src/entities/player.py** - Игрок:
- Позиция, движение
- Спрайты (idle/walk)
- Коллизии

**src/entities/npc.py** - NPC:
- Отец (Father)
- Мама (Mother)
- Диалоги
- Радиус взаимодействия

**src/scenes/** - Сцены:
- `base_scene.py` - Базовый класс сцены
- `cave.py` - Пещера (фон, стены, выходы)
- `kitchen.py` - Кухня (фон, декорации)
- `minigame.py` - Мини-игра:
  - Класс Monkey (обезьяна)
  - Класс Poop (какашка)
  - Поляна и кусты
  - Логика движения обезьян
  - Броски какашек

**src/systems/** - Системы:
- `health_system.py` - HP система
- `energy_system.py` - Энергия
- `progression_system.py` - Прокачка (XP, уровни, улучшения)

**src/ui/** - UI:
- `hud.py` - HUD (HP, энергия, уровень, время выживания)
- `dialog_box.py` - Диалоговое окно с опциями

**src/services/** - Сервисы:
- `save_system.py` - Система сохранений
- `brainlink_launcher.py` - BrainLink (опционально)

### 4. Сохранения

```
saves/
└── game_save.json            # Сохранение игры
```

Формат сохранения:
- Позиция игрока
- Текущая сцена
- HP, энергия
- Уровень, XP
- Улучшения
- Лучшее время в мини-игре

## Логика игры

### Переходы между локациями:

1. **Пещера → Кухня**: Автоматически при подходе к выходу справа
2. **Пещера → Мини-игра**: 
   - При подходе к выходу слева отец перехватывает
   - Показывает диалог: "Одному выходить нельзя! Давай лучше поиграем - там обезьяны снаружи!"
   - Кнопка: "Играть в мини-игру"
   - После выбора показывается диалог выбора позиции (Север/Юг/Запад/Восток)
3. **Мини-игра → Пещера**: Автоматически при подходе к выходу

### Мини-игра:

**Стартовые позиции:**
- North: (0, 15) - только влево/вправо
- South: (0, -15) - только влево/вправо
- West: (-15, 0) - только вверх/вниз
- East: (15, 0) - только вверх/вниз

**Поляна:**
- Размер: 25x20 единиц
- Позиция зависит от стартовой позиции (всегда перед персонажем)
- Светло-зелёная

**Кусты:**
- Окружают поляну с других сторон
- Тёмно-зелёные

**Обезьяны:**
- Спавнятся в кустах (радиус ~30)
- Выбегают на поляну
- Бегают по полю
- Кидают какашки в игрока
- Характеристики по возрастам (см. balance.json)

**Какашки:**
- Летят к игроку с учётом точности обезьяны
- Скорость зависит от возраста обезьяны
- При попадании отнимают 1 HP

### Система прокачки:

- XP за выживание (10 XP/сек)
- XP за уклонение от какашек (5 XP/уклонение)
- Улучшения:
  - max_hp: +1 HP за уровень
  - max_energy: +10 энергии за уровень
  - energy_regen: +1.0 реген за уровень
  - move_speed: +20 скорости за уровень

## Рекомендации для Godot

### Структура проекта Godot:

```
godot_project/
├── scenes/
│   ├── MainMenu.tscn
│   ├── Cave.tscn
│   ├── Kitchen.tscn
│   └── Minigame.tscn
├── scripts/
│   ├── player.gd
│   ├── npc.gd
│   ├── monkey.gd
│   ├── poop.gd
│   ├── systems/
│   │   ├── health_system.gd
│   │   ├── energy_system.gd
│   │   └── progression_system.gd
│   └── ui/
│       ├── hud.gd
│       └── dialog_box.gd
├── assets/
│   └── (скопировать все спрайты)
├── config/
│   └── (скопировать JSON файлы)
└── saves/
    └── (создать папку)
```

### Важные моменты:

1. **Ортогональная камера** с aspect ratio 16:9
2. **Разрешение**: 1920x1080 (настраиваемое)
3. **FPS**: 60
4. **Система координат**: 2D, игрок в центре, обезьяны вокруг
5. **Коллизии**: Использовать Area2D для выходов и взаимодействий
6. **Диалоги**: UI с кнопками опций
7. **Сохранения**: JSON файлы (как в оригинале)

### Порядок миграции:

1. Скопировать все ассеты (спрайты)
2. Скопировать конфигурационные файлы
3. Создать базовую структуру сцен
4. Реализовать игрока и движение
5. Реализовать NPC и диалоги
6. Реализовать локации (Пещера, Кухня)
7. Реализовать мини-игру (обезьяны, какашки, поляна)
8. Реализовать системы (HP, энергия, прокачка)
9. Реализовать UI (HUD, диалоги)
10. Реализовать сохранения

## Дополнительные файлы

- `main.py` - Точка входа (для справки)
- `requirements.txt` - Зависимости Python (не нужны для Godot)
- `README.md` - Описание проекта
- `CHANGELOG.md` - История изменений

## Примечания

- BrainLink интеграция опциональна (можно пропустить)
- Все спрайты можно заменить на собственные
- Баланс можно настроить через balance.json
- Система сохранений использует JSON (легко портировать)
